{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI Uvicorn",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                // "--reload",
                // "--env-file",
                // "${workspaceFolder}/.env",
                // "--port",
                // "5006"
            ],
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}
 